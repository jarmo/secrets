dependencies:
  post:
    - curl -L https://github.com/golang/dep/releases/download/v0.5.0/dep-linux-amd64 -o dep
    - chmod +x dep
    - ./dep ensure -no-vendor -dry-run
test:
  override:
    - make test
